# UDP Server Listen Port
server.port=9876

# SMS limit for send to SMS Client
outbox.limit=50

# Cron expression for alarm trigger, default every minutes
scheduler.cron=0 */1 * * * *

# Maximum trigger max exceed in minutes
alarm.tolerance=5

# Maximum node to be fethced for alarm trigger, 0 = unlimited
node.limit=100
